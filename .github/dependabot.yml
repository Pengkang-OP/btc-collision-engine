# Dependabot 自动依赖更新配置
# 文档: https://docs.github.com/en/code-security/dependabot

version: 2
updates:
  # Python 依赖
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "Asia/Shanghai"
    open-pull-requests-limit: 10
    labels:
      - "dependencies"
      - "automated"
    commit-message:
      prefix: "chore"
      prefix-development: "chore"
      include: "scope"
    # 忽略次要/补丁版本的主依赖更新（减少噪音）
    ignore:
      - dependency-name: "numpy"
        update-types: ["version-update:semver-patch"]
      - dependency-name: "pyopencl"
        update-types: ["version-update:semver-patch"]
    # 非 dev 依赖分组，减少 PR 数量
    groups:
      production-dependencies:
        dependency-type: "production"
        update-types:
          - "minor"
          - "patch"
      development-dependencies:
        dependency-type: "development"
        update-types:
          - "minor"
          - "patch"

  # GitHub Actions 自身依赖
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "Asia/Shanghai"
    labels:
      - "dependencies"
      - "automated"
    commit-message:
      prefix: "chore"
      include: "scope"
