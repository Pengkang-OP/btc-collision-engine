# BTC 私钥碰撞工具依赖
# 核心依赖
coincurve>=18.0.0       # libsecp256k1 绑定（推荐，性能最佳）
psutil>=5.9.0           # 系统资源监控

# 密码学库
cryptography>=41.0.0,<47.0.0    # 密钥安全清零(推荐后端)、常量时间比较
PyNaCl>=1.5.0,<2.0.0            # libsodium绑定(sodium_memzero安全清零)、防侧信道攻击

# 测试框架
pytest>=8.0.0

# 开发工具
black>=24.0.0
flake8>=6.0.0
mypy>=1.0.0

# 性能优化
numba>=0.60.0
numpy>=1.20.0           # Numba 依赖

# 性能优化 - 新增 (v2.2.0)
gmpy2>=2.1.5            # 大整数运算优化(Comba乘法),模运算性能+35%
pycryptodome>=3.19.0    # SIMD哈希优化(AES-NI),SHA256性能+200%
memory-profiler>=0.61   # 内存性能分析工具(可选)
pytest-benchmark>=4.0   # 性能基准测试框架(可选)

# 目标地址管理增强
cachetools>=5.3.0       # LRU/TTL缓存，加速地址解析
pybloom-live>=2.2.0     # 布隆过滤器，优化大规模地址去重
bech32>=1.2.0           # Bech32地址解析支持（可选）
bitarray>=2.6.0         # Bloom过滤器底层位数组实现，去重功能核心依赖
chardet>=5.0.0,<6.0.0  # 文件编码自动检测，跨平台字符集兼容性

# 监控与通知
requests>=2.28.0,<3.0.0 # HTTP请求库，告警通知Webhook发送

# 可选工具依赖
ecdsa>=0.18.0           # 椭圆曲线签名库，备用加密后端（可选，coincurve可替代）
setproctitle>=1.3.0     # 多进程进程名设置，便于系统监控（可选）

# 文档工具
sphinx>=7.0.0
sphinx-rtd-theme>=1.3.0

# GPU加速依赖 (v2.2.1)
pyopencl>=2021.1        # OpenCL GPU 计算支持（必需）
