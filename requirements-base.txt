# BTC 碰撞引擎 - 基础必选依赖
# 安装: pip install -r requirements-base.txt

# Python 版本要求
# 需要 Python 3.12+ 以支持:
# - PEP 695 类型参数语法 (def func[T](x: T) -> T)
# - PEP 701 f-string 跨行表达式
# - PEP 692 TypedDict **kwargs 余量
# - itertools.batched() / pathlib.walk() 等标准库增强
# - datetime.UTC 时区常量
# 
# 在 pyproject.toml 中设置:
#   requires-python = ">=3.12"

# 加密核心
cffi>=1.15.0,<3.0.0          # 含上限 <3.0.0 与 pyproject.toml 统一；如需 Python 3.14 预编译可临时移除
coincurve>=18.0.0,<22.0.0 # libsecp256k1 绑定（强烈推荐，性能最佳）
cryptography>=43.0.0,<46.0.0  # C-03: 放宽版本约束，兼容 44.x/45.x
PyNaCl>=1.5.0,<2.0.0        # libsodium 绑定，防侧信道攻击
gmpy2>=2.1.0,<5.0.0         # C-07: GMP 大整数运算，放宽上限与 pyproject.toml 统一
pycryptodome>=3.19.0,<5.0.0 # C-08: SIMD 哈希优化（AES-NI），预留 4.x 兼容性

# 系统工具
psutil>=5.9.0,<8.0.0        # 系统资源监控
numpy>=1.24.0,<2.0.0         # 数值计算，SIMD 优化支持（锁定 1.x，与 pyproject.toml 一致）

# 地址解析
cachetools>=5.3.0,<7.0.0      # LRU/TTL 缓存，加速地址解析
pybloom-live>=2.2.0,<5.0.0    # Bloom 过滤器，大规模地址去重
bech32>=1.2.0,<2.0.0          # Bech32 地址解析（工具脚本备用；核心引擎使用内置 bech32_codec）
bitarray>=2.6.0,<4.0.0        # Bloom 过滤器底层位数组
chardet>=5.0.0,<6.0.0         # 文件编码自动检测

# 网络通信
requests>=2.28.0,<3.0.0       # HTTP 请求，告警通知 Webhook

# 可选工具
ecdsa>=0.18.0,<1.0.0          # 纯 Python ECDSA 备用后端
setproctitle>=1.3.0,<2.0.0    # 进程名设置，便于系统监控

# 配置验证（H-NEW1修复）
jsonschema>=4.0.0,<5.0.0      # 配置文件 JSON Schema 验证

# CLI 输出美化
rich>=13.0,<16.0.0            # 富文本输出：Panel/Table/Rule/颜色控制，管道模式自动禁用 ANSI
